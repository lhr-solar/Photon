include(${CMAKE_SOURCE_DIR}/find/embed_assets.cmake)

set(ENVIRONMENT_DIR ${CMAKE_CURRENT_SOURCE_DIR})
set(ENVIRONMENT_HEADER_OUTPUT_DIR ${CMAKE_BINARY_DIR}/generated)
set(ENVIRONMENT_OBJECT_OUTPUT_DIR ${CMAKE_BINARY_DIR}/generated/environment_objects)

set(ENVIRONMENT_FILES
    ${ENVIRONMENT_DIR}/background.jpg
    ${ENVIRONMENT_DIR}/studio.hdr
)

photon_embed_assets(
    HEADER_OUTPUT_DIR ${ENVIRONMENT_HEADER_OUTPUT_DIR}
    OBJECT_OUTPUT_DIR ${ENVIRONMENT_OBJECT_OUTPUT_DIR}
    HEADER_TARGET EnvironmentHeaders
    ASSET_TARGET EnvironmentAssets
    FILES ${ENVIRONMENT_FILES}
)
